{
  "name": "dabbleduck-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Optional local-first LAN family server for DabbleDuck. Stores and syncs profiles, progress, and creations across the family's devices. Never required for local play.",
  "license": "MIT",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
